import React from "react";
import styled from "styled-components";

const InBanner = styled.div`
  font-size: 1em;
  text-align: center;
`;

const InfoBrand = () => {
  return <InBanner>Input the verify code to activate your account.</InBanner>;
};

export default InfoBrand;
